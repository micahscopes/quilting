# HHHS 0.4.5 `7575510` candidate adoption

Date: 2026-09-01

Candidate: `7575510b7f8d95588c39cc4d0f22bd77a1979760`

This exact-graph advance adds the upstream executable browser/session protocol
oracle on top of `3968ae1`. It is an immutable development pin, not the HHHS
0.4.5 release tag.

The oracle keeps these observations distinct:

1. bounded application-sideband acceptance;
2. packet authentication and replay-state durability;
3. causal readiness and reversible prediction;
4. exact capability-presented entry admission;
5. durable confirmation;
6. projection revision continuity and reset on a missed notice;
7. public-record announcement;
8. late repair to an equal canonical history root.

That is the contract Hyperscope should follow. It does not justify a second
session implementation or moving carriers, browser resources, render loops,
or application semantics into HHHS.

Upstream's complete workspace/doctest suite, WASM cross-build, strict Clippy,
and strict rustdoc passed before the commit was pushed. Downstream qualification
repeats the HHHS-dependent native and WASM composition gates on this exact SHA.

[build]
target = "remesh-lab.html"
dist = "dist-lab"

[serve]
port = 9090

[watch]
watch = [
    "remesh-lab.html",
    "crates/quilting-wasm/src/",
    "crates/quilting-remesh/src/",
]

[[hooks]]
stage = "pre_build"
command = "bash"
command_arguments = ["scripts/build-remesh-lab-wasm.sh"]

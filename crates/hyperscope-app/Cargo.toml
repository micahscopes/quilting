[package]
name = "hyperscope-app"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Rust-authoritative application reducer and FRP read models for Hyperscope"

[features]
default = []
wire = ["dep:serde"]
replay = ["wire", "dep:serde_json", "uuid/serde"]

[dependencies]
futures-signals = { version = "0.3.34", default-features = false }
hyperscape = { version = "0.1.0", path = "../hyperscape" }
hyperscape-protocol = { version = "0.1.0", path = "../hyperscape-protocol" }
quilting-core = { version = "0.1.0", path = "../quilting-core" }
quilting-gltf = { version = "0.1.0", path = "../quilting-gltf" }
serde = { version = "1", features = ["derive"], optional = true }
serde_json = { version = "1", features = ["float_roundtrip"], optional = true }
uuid = "1"

[[bin]]
name = "hyperscope-replay"
path = "src/bin/hyperscope-replay.rs"
required-features = ["replay"]

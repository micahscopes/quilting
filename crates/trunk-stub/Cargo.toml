[package]
name = "trunk-stub"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Placeholder cdylib so Trunk has a crate to build; the real WASM comes from the wasm-pack hook"

[lib]
crate-type = ["cdylib"]
